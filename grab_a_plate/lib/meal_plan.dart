import 'package:flutter/material.dart';

class MealPlan extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Meal Plan Screen', style: TextStyle(fontSize: 24)),
    );
  }
}
